spring:
  application:
    name: springFlix
  datasource:
    url: jdbc:postgresql://localhost:5432/app
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: true
  flyway:
    enable: true
    locations: classpath:db/migration

